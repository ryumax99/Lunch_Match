<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lunchmatch.mapper.CommentMapper">

    <insert id="insertComment">
        INSERT INTO comment (party_id, writer_id, content, gender)
        VALUES (#{partyId}, #{writerId}, #{content}, #{gender})
    </insert>

    <select id="selectCommentsByPartyId" resultType="com.lunchmatch.domain.CommentVO">
        SELECT * FROM comment
        WHERE party_id = #{partyId}
        ORDER BY created_at DESC
    </select>

    <select id="getCommentsByPartyId" resultType="com.lunchmatch.domain.CommentVO">
        SELECT *
        FROM comment
        WHERE party_id = #{partyId}
        ORDER BY created_at ASC
    </select>

    <delete id="deleteByPartyId">
        DELETE FROM comment WHERE party_id = #{partyId}
    </delete>

    <delete id="deleteById">
        DELETE FROM comment WHERE id = #{id}
    </delete>

    <update id="updateComment">
        UPDATE comment
        SET content = #{content}
        WHERE id = #{id}
    </update>
</mapper>
